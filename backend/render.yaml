services:
  - type: web
    name: rental-api
    rootDir: backend                      # 👈 Tells Render to use this directory
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    plan: free
